<section class="hero">
  <div class="preview-badge">preview</div>
  <div class="texture-overlay"></div>
  <div class="container">
    <div class="logo">
      <img src="/logo.svg" alt="PowerExplorer Logo" width="80" height="80" />
    </div>
    <h1>PowerExplorer</h1>
    <p class="tagline">
      Modern alternative of a popular <span>Fetch XML Builder</span> tool.
    </p>

    <div class="tags">
      <div class="tag">modern tech stack</div>
      <div class="tag">friendly UI</div>
      <div class="tag">fast responses</div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 100px 0 80px;
    text-align: center;
    position: relative;
    background-color: #eaeaea;
  }

  .texture-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("src/images/hero-texture.png");
    background-repeat: repeat;
    z-index: 1;
  }

  .container {
    position: relative;
    z-index: 2;
  }

  .preview-badge {
    position: absolute;
    top: 20px;
    right: 20px;
    background-color: var(--accent-orange);
    color: var(--white);
    padding: 4px 12px 5px 12px;
    border-radius: 20px;
    font-size: 14px;
    font-weight: 400;
    z-index: 3;
  }

  .logo {
    margin: 0 auto 30px;
    display: flex;
    justify-content: center;
  }

  .logo img {
    width: 170px;
  }

  h1 {
    font-size: 140px;
    margin-bottom: 20px;
    letter-spacing: -2px;
    font-family: "Sora", sans-serif;
    font-weight: 800;
  }

  .tagline {
    font-size: 24px;
    color: var(--gray-700);
    margin-bottom: 30px;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .tagline span {
    font-weight: 600;
  }

  .tags {
    display: flex;
    gap: 12px;
    justify-content: center;
    margin-top: 40px;
  }

  .tag {
    background-color: var(--gray-900);
    color: var(--white);
    padding: 8px 16px;
    border-radius: 50px;
    font-size: 14px;
  }

  /* Media queries */

  @media (max-width: 1024px) {
    .hero {
      padding: 100px 0 60px;
    }
    .logo img {
      width: 140px;
    }
    h1 {
      font-size: 120px;
    }
  }

  @media (max-width: 992px) {
    .hero {
      padding: 100px 0 60px;
    }
    .logo img {
      width: 100px;
    }

    h1 {
      font-size: 90px;
    }

    .tagline {
      font-size: 22px;
    }
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 80px;
    }

    .tagline {
      font-size: 20px;
    }

    .logo img {
      width: 120px;
    }
  }

  @media (max-width: 576px) {
    .hero {
      padding: 80px 0 50px;
    }

    h1 {
      font-size: 40px;
      letter-spacing: -1px;
    }

    .tagline {
      font-size: 18px;
    }

    .tags {
      flex-direction: column;
      gap: 10px;
      align-items: center;
    }

    .preview-badge {
      top: 10px;
      right: 10px;
      font-size: 12px;
      padding: 3px 10px;
    }
  }
</style>

